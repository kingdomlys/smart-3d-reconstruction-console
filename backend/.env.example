# Server
APP_HOST=0.0.0.0
APP_PORT=8000

# Data
TASKS_ROOT=./data/tasks

# Optional: TripoSR worker python path override
# TRIPOSR_PY=C:/path/to/triposr/python.exe

# Optional: TripoSR command line hook (input_path output_path)
# TRIPOSR_CMD=python path/to/triposr_infer.py

# Optional: enable/disable rembg preprocessing
# USE_REMBG=1

# Optional: COLMAP command hook (expects --input-dir and --output-dir)
# COLMAP_CMD=python backend/workers/colmap_pipeline.py
# COLMAP_BIN=colmap

# Optional: 3DGS command hook (expects --interim-dir, --output-dir, --iterations)
# DGS_CMD=python backend/workers/gsplat_pipeline.py
# DGS_TRAIN_SCRIPT=path/to/train.py
